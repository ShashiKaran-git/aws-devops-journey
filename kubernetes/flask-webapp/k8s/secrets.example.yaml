cat > kubernetes/flask-webapp/k8s/secrets.example.yaml << 'EOF'
apiVersion: v1
kind: Secret
metadata:
  name: flask-secret
  namespace: flask-app
  labels:
    project: flask-webapp
type: Opaque
data:
  DB_PASSWORD: <your-base64-encoded-password>
  SECRET_KEY: <your-base64-encoded-secret-key>
EOF
