apiVersion: v1
kind: Service
metadata:
  name: flask-webapp
  namespace: flask-app
  labels:
    project: flask-webapp
spec:
  type: NodePort
  selector:
    app: flask-webapp
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
